{
  "compilerOptions": {
    "target": "esnext",  // 选择一个适合的目标 ECMAScript 版本
    "module": "esnext",  // 使用 ES 模块语法
    "moduleResolution": "node",  // 确保模块解析方式使用 Node 风格
    "strict": true,  // 启用严格模式，帮助避免潜在错误
    "esModuleInterop": true,  // 支持导入 CommonJS 模块
    "skipLibCheck": true,  // 跳过库的类型检查以提高构建速度
    "allowJs": true,  // 如果你同时有 JavaScript 文件，也可以允许它们
    "outDir": "dist",  // 输出目录
    "resolveJsonModule": true  // 支持 JSON 文件导入
  },
  "include": [
    "src/**/*.ts",  // 包含所有 TypeScript 文件
    "src/**/*.d.ts" , // 如果有类型定义文件
    "node_modules/three/**/*" 
  ]
}
